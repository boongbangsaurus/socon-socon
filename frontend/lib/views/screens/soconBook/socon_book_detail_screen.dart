import 'package:flutter/material.dart';

class SoconBookDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("소콘북 디테일 화면"),
    );
  }
}
